import {
  Button,
  Field,
  Cell,
  CellGroup,
  DatetimePicker,
  Popup,
  Dialog,
  Toast,
  Grid,
  GridItem,
  Row,
  Col
} from "vant"

function install(){
  this.use(Button)
  this.use(Field)
  this.use(CellGroup)
  this.use(DatetimePicker)
  this.use(Popup)
  this.use(Cell)
  this.use(Dialog)
  this.use(Toast)
  this.use(Grid)
  this.use(GridItem)
  this.use(Row)
  this.use(Col)

}

export default install
