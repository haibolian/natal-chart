import {
  Button,
  Field,
  Cell,
  CellGroup,
  DatetimePicker,
  Popup,
  Dialog
} from "vant"

function install(){
  this.use(Button)
  this.use(Field)
  this.use(CellGroup)
  this.use(DatetimePicker)
  this.use(Popup)
  this.use(Cell)
  this.use(Dialog)

}

export default install
